file(GLOB SOURCES "*.cpp")
file(GLOB HEADERS "${LIBBITCOIN_INCLUDE_DIR}/chain/*.hpp")
add_library(libbitcoin_chain OBJECT	${SOURCES} ${HEADERS})

if(MSVC)
    target_compile_options(libbitcoin_chain PUBLIC "/wd4245")
    target_compile_options(libbitcoin_chain PUBLIC "/wd4505")
    target_compile_options(libbitcoin_chain PUBLIC "/wd4706")
    target_compile_options(libbitcoin_chain PUBLIC "/wd4996")
    target_compile_options(libbitcoin_chain PUBLIC "/wd4273")
    target_compile_options(libbitcoin_chain PUBLIC "/wd4251")
elseif(APPLE)
    target_compile_options(libbitcoin_chain PUBLIC -Wno-mismatched-tags)
    target_compile_options(libbitcoin_chain PUBLIC -Wno-unused-variable)
    target_compile_options(libbitcoin_chain PUBLIC -Wno-unused-lambda-capture)
    target_compile_options(libbitcoin_chain PUBLIC -Wno-mismatched-tags)
    target_compile_options(libbitcoin_chain PUBLIC -Wno-reorder)
    target_compile_options(libbitcoin_chain PUBLIC -Wno-missing-braces)
    target_compile_options(libbitcoin_chain PUBLIC -Wno-tautological-compare)
elseif(ANDROID)
    target_compile_options(libbitcoin_chain PUBLIC -Wno-tautological-constant-compare)
    target_compile_options(libbitcoin_chain PUBLIC -Wno-reorder)
    target_compile_options(libbitcoin_chain PUBLIC -Wno-unused-variable)
    target_compile_options(libbitcoin_chain PUBLIC -Wno-mismatched-tags)
    target_compile_options(libbitcoin_chain PUBLIC -Wno-unused-lambda-capture)
else()
    target_compile_options(libbitcoin_chain PUBLIC -fpermissive)
    target_compile_options(libbitcoin_chain PUBLIC -Wno-reorder)
    target_compile_options(libbitcoin_chain PUBLIC -Wno-sign-compare)
    target_compile_options(libbitcoin_chain PUBLIC -Wno-unused-variable)
endif()