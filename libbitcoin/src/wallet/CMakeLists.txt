file(GLOB SOURCES "*.cpp")
file(GLOB PARSE_ENCRYPTED_SOURCES "parse_encrypted_keys/*.cpp")
file(GLOB HEADERS "${LIBBITCOIN_INCLUDE_DIR}/wallet/*.hpp")

add_library(libbitcoin_wallet OBJECT ${SOURCES} ${PARSE_ENCRYPTED_SOURCES} ${HEADERS})

if(MSVC)
    target_compile_options(libbitcoin_wallet PRIVATE "/wd4244")
    target_compile_options(libbitcoin_wallet PRIVATE "/wd4245")
    target_compile_options(libbitcoin_wallet PRIVATE "/wd4505")
    target_compile_options(libbitcoin_wallet PRIVATE "/wd4458")
    target_compile_options(libbitcoin_wallet PRIVATE "/wd4996")
    target_compile_options(libbitcoin_wallet PRIVATE "/wd4251")
else()
    target_compile_options(libbitcoin_wallet PRIVATE -Wno-mismatched-tags)
    target_compile_options(libbitcoin_wallet PRIVATE -Wno-unused-variable)
    target_compile_options(libbitcoin_wallet PRIVATE -Wno-reorder)
endif()